<div class="container">
  <div class="row">
    <div class="ten columns">
      <% if @group.admin == current_user.id %>
        <%= render partial: "groups/edit_form", locals: { group: @group } %>
      <% else %>
        <h3><%= @group.name %></h3>
        <p>Budget: <%= number_to_currency(@group.budget) %></p>
      <% end %>
    </div>
    <div class="two columns">
      <%= render partial: "shared/swapboard_link", locals: { group: @group } %>
      <% if @group.admin == current_user.id %>
        <p><%= link_to "Manage swappers", group_members_path(@group.name) %></p>
      <% end %>
    </div>
  </div>
</div>